# AGENTS.md — AI協働の共通ルール（正本）

このファイルが、このリポジトリにおけるAIエージェント運用ルールの**唯一の正本**です。
Codex は本ファイルを直接読み込みます。Claude Code は `CLAUDE.md`（中身は `@AGENTS.md` の1行）経由で同じ内容を読みます。
ルールを変更するときは、必ず本ファイルだけを編集してください。

対象エージェント: **Codex** / **Claude Code**

---

## 0. ゴール

**Codex で作業を止めて Claude Code で再開しても、その逆でも、人間からの状況説明なしに続きができる状態を保つこと。**

このルールの各項目は、すべてこの1点のために存在します。

---

## 1. 大原則

### 原則1：共有状態は push 済みの GitHub だけ

Codex・Claude Code のいずれも、クラウド実行モードでは指定ブランチを clone / checkout した内容しか見えません。
**Mac ローカルにしか存在しない未 push の変更は、相手からは存在しないのと同じです。**

したがって、引き継ぎに必要な情報は例外なく commit して push します。ローカル専用フォルダに置かないでください。

### 原則2：正本はひとつ

同じ内容を2ヶ所に持たない。コピーによる同期はしない（必ずズレるため）。
ルールは `AGENTS.md`、スキル実体は `.claude/skills/`。参照側は symlink または import で繋ぎます。

### 原則3：担当と進捗は GitHub Issue、決定理由は PR

Markdown ファイルで担当表を管理しない。Issue と PR が排他・履歴・タイムスタンプを持っています。
`HANDOFF.md` は「今どこにいるか」を示す**現在地のみ**を担い、履歴は持ちません。

---

## 2. セッション開始時プロトコル

新しいセッションはデフォルトブランチ（`main`）を開くため、**作業中のブランチ上の `HANDOFF.md` は見えません。**
`HANDOFF.md` は自分自身の在り処を教えられないので、開始時は必ず Issue から入ります。

1. **open な Issue を確認する**（`main` にいても読めるため、ここが唯一の確実な入口）
2. 進行中の Issue があれば、最新コメントを読む
   - 「担当AI」「作業ブランチ」「開始時刻」が書かれています
   - **他方のAIが作業中と読み取れる場合は、勝手に着手せず人間に確認する**
3. Issue に書かれた作業ブランチを checkout する
4. そのブランチの `HANDOFF.md` を読む
5. 作業を開始する前に、Issue へ着手コメントを書く（次項）

## 3. 作業中のルール

### ブランチ

作業は必ず feature ブランチで行い、`main` に直接コミットしない。

- Claude Code: `claude/<内容>-<識別子>`
- Codex: `codex/<内容>`

### Issue への着手コメント（作業開始時に必ず書く）

GitHub Issue は厳密な排他ロックではないため、宣言で衝突を防ぎます。

```
担当AI: Codex
作業ブランチ: codex/add-config-file
開始時刻: 2026-08-26 14:30 JST
```

着手前に最新コメントを確認し、他方が作業中でないことを確かめてから書いてください。

### 決定の記録

設計上の判断とその理由は、PR の説明文に書きます。独立したファイルには残しません。
（PR を読み返しても分からない横断的な設計判断が3件たまった時点で、`DECISIONS.md` の新設を検討します）

---

## 4. セッション終了時プロトコル

**この順序を厳守してください。順序に意味があります。**

1. **`HANDOFF.md` を更新する**（上書き。追記しない）
2. 中断する場合は、GitHub Issue に進捗コメントを書く
3. `git commit`
4. `git push`
5. 必要に応じて PR を作成 / 更新する

> ⚠️ **1 を 3・4 より先に行う理由**
> push の後に `HANDOFF.md` を更新すると、その更新は commit に含まれず、相手側に永久に届きません。
> `HANDOFF.md` の更新は「push する前の作業」ではなく「**push される中身そのもの**」です。

作業が未完了のまま終わる場合でも、この5ステップは必ず実行します。
「中途半端だから push しない」は、相手から見ると**作業が消えた**のと同じです。

---

## 5. HANDOFF.md のフォーマット

以下の形式に従ってください。常に上書きし、**20行以内**に保ちます。

```markdown
# HANDOFF

- 更新: YYYY-MM-DD HH:MM JST
- 更新者: Codex | Claude Code
- 対象Issue: #12
- 作業ブランチ: codex/add-config-file
- PR: #13 （未作成なら「なし」）
- 状態: 作業中 | レビュー待ち | 完了

## いま何をしていたか
（1〜3行）

## 次にやること
（箇条書き。最大3つ）

## 詰まっている点・人間の判断が必要な点
（なければ「なし」）
```

**Issue・ブランチ・PR番号の3つは必須です。** 再開側がこれだけで現場に到達できることが、このファイルの唯一の役割です。

---

## 6. ファイル構成と役割

| パス | 役割 | 更新頻度 |
|---|---|---|
| `AGENTS.md` | 共通ルールの正本。両AIが読む | ルール変更時のみ |
| `CLAUDE.md` | `@AGENTS.md` の1行のみ。Claude Code の読み込み口 | 変更しない |
| `HANDOFF.md` | 現在地のみ。上書き運用 | セッション終了ごと |
| `.claude/skills/` | スキルの**実体**（正本） | スキル追加時 |
| `.agents/skills` | `../.claude/skills` への symlink（Codex 用の参照口） | 変更しない |

## 7. スキル

スキルの実体は `.claude/skills/<スキル名>/SKILL.md` に置きます。
形式は Markdown + YAML frontmatter で、Codex・Claude Code の双方が解釈できます。

`.agents/skills` は `.claude/skills` への symlink です。実体をコピーしないでください。

> **検証中の項目**：Codex のスキル探索が symlink 先を確実に辿るという公式保証は未確認です。
> このリポジトリで新規 Codex セッションを起動し、`handoff` スキルが認識されることを確認してから、
> 他リポジトリへ横展開してください。認識されない場合は、この構成を見直します。

## 8. 実行環境の違い（早見表）

| | ローカル実行 | クラウド実行 |
|---|---|---|
| **Codex** | Mac 上のファイルが見える | 選択したブランチ / コミットを checkout。未 push は見えない |
| **Claude Code** | Mac 上のファイルが見える | リポジトリを新規 clone。未 push は見えない。セッション終了でコンテナ破棄 |

いずれのクラウド実行も、**push 済みの GitHub が唯一の共通基準**です。

---

## 9. このリポジトリについて

ターミナルで動くポモドーロタイマー（Python 3、単一ファイル `pomodoro.py`）。
通知機能は macOS の `osascript` に依存しており、他OSでは動作しません。

実行方法:

```bash
python3 pomodoro.py
```
